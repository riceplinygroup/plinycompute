# create the target
add_pdb_integration_test(TestConsecutiveOptimizedJoin)

# add a build dependency to build-tests target
add_dependencies(build-integration-tests TestConsecutiveOptimizedJoin)

# add build dependencies to shared libraries it uses
add_dependencies(TestConsecutiveOptimizedJoin OptimizedMethodJoin)
add_dependencies(TestConsecutiveOptimizedJoin ScanStringIntPairSet)
add_dependencies(TestConsecutiveOptimizedJoin StringSelectionOfStringIntPair)
add_dependencies(TestConsecutiveOptimizedJoin WriteStringSet)